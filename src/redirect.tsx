import axios from 'axios';

import { useQuery } from '@tanstack/react-query';
import { useEffect } from 'react';

type ParamsType = {
    params: string;
};

// interface Data {
//     full_url: string | URL;
// }

function RedirectPage({ params }: ParamsType) {
    RedirectPage;
   
}

export default RedirectPage;
